#!/bin/zsh

cat ~/workspace/ai-platform-sre-knowledge-base/inventory/all_mle_servers.json \
   | jq -r '.result[] | select ((.u_lifecycle != "decommissioned") and (.operational_status != "6")) | [.host_name]|@csv' | column -t -s, | sed 's/\"//g'
