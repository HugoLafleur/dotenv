# !/bin/bash

if [ -f ~/.osx_functions  ]; then
  . ~/.osx_functions
fi

alias edit='open_with_sublime_text'

alias ip=ifconfig |grep -A4 ^en3|tail -1|cut -d' ' -f2
alias wip=ifconfig |grep -A3 ^en0|tail -1|cut -d' ' -f2
